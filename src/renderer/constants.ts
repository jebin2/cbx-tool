export const IMAGE_EXTENSIONS = [".jpg", ".jpeg", ".png", ".gif", ".webp", ".bmp"];

export const COPY_BTN_DEFAULT_LABEL = "Copy";
export const COPY_BTN_DEFAULT_TITLE = "Copy current page image to clipboard";

// Distance (px) from the edge of the page list that triggers auto-scroll during drag
export const DRAG_SCROLL_THRESHOLD = 60;
// Maximum pixels scrolled per animation frame during drag
export const DRAG_SCROLL_MAX_SPEED = 15;
// How long (ms) the copy-button success/error feedback stays visible
export const COPY_FEEDBACK_DURATION_MS = 1600;
// How far (px) into the next/prev image the viewer must scroll before switching pages
export const PAGE_SWITCH_SCROLL_THRESHOLD = 200;
// Delay (ms) before clearing the programmatic-scroll guard flag after a scroll call
export const SCROLL_FLAG_RESET_DELAY_MS = 50;
